/**
 * Where the card actually is.
 *
 * The three "Get Coma Card" controls used to scroll to a section of this same page, which explains
 * the product to somebody who has already decided to use it. They open the app now.
 *
 * A new tab rather than a navigation: the landing page is the thing a visitor was sent, often from
 * a chat or a submission, and taking it away from them to open an app is the wrong trade.
 *
 * One definition because three controls use it, in two files. Two copies of a URL is two chances
 * for one of them to point at last week's deployment.
 */
export const APP_URL = "https://app.comacard.xyz";
