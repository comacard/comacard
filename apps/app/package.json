{
  "name": "@comacard/app",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Cardholder PWA: scan QRIS and spend",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "build": "bun build src/index.ts --target=bun --outdir=dist",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist .turbo"
  },
  "devDependencies": {
    "@comacard/tsconfig": "workspace:*",
    "typescript": "^5.9.3"
  }
}
