import { SendToAddress } from "../../../../components/send/SendToAddress";

/**
 * The draw plus a transfer. Two transactions, presented as two — see the component for why they
 * cannot be one.
 */
export default function SendToPage() {
  return <SendToAddress />;
}
