{
  "name": "@comacard/kyc",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "KYC service: Didit session orchestration and webhook intake",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "build": "bun build src/index.ts --target=bun --outdir=dist",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist .turbo kyc.sqlite"
  },
  "devDependencies": {
    "@comacard/tsconfig": "workspace:*",
    "typescript": "^5.9.3"
  }
}
