# HyperSync access, needed for the Sepolia chain. Create one at
# https://envio.dev/app/api-tokens — Creditcoin syncs over plain RPC and needs
# no token, so a Creditcoin-only run works without this.
ENVIO_API_TOKEN=""
