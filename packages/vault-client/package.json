{
  "name": "@sorosense/vault-client",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build:bindings": "tsc -p bindings",
    "pretypecheck": "tsc -p bindings",
    "typecheck": "tsc --noEmit",
    "pretest": "tsc -p bindings",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  },
  "dependencies": {
    "@stellar/stellar-sdk": "^14.6.1",
    "buffer": "6.0.3"
  }
}
